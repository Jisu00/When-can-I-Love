const HOME = "/";
const LOADING = "/loading";
const QUESTION = "/question";
const RESULT = "/result";

const routes = {
  home: HOME,
  loading: LOADING,
  question: QUESTION,
  result: RESULT,
};

export default routes;
